# References

Pointers to external truth — not a copy of it. If you find yourself pasting
content from one of these sources into this file, stop and just link to it.

## GitHub

- Org/repo: `Kiloma-Advanced-Solutions/mft-rsvp`.
- Issues: use `gh issue list` / `gh issue view <n>` rather than duplicating
  issue content here.
- PR history: use `gh pr list` / `gh pr view <n>`. Don't restate merged PR
  content in this file — it rots the moment the PR is updated.

## Brand

- Kiloma-branded workshop brief PDF: `docs/KAS-WS-0014-01-Events-Board-Workshop-Brief.pdf`
  (added in commit `27ab7be`). This is the branded/presentation version of
  the brief — `TASKS.md` is the working copy to actually build against.

## Brief / source of truth

- `TASKS.md` is the canonical spec for this project (what to build, the
  permission rules in §4, milestones in §5, PR template in §8).

## Design files

- None yet. Add a pointer here if a Figma/design file becomes the source
  of truth for visual work — don't just say "ask Yaniv".
